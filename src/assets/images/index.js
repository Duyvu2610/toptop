const images = {
    logo: require('./logo.svg').default,
    noImg: require('./no-image.png'),
    upload: require('./upload.svg').default,
    logoImg: require('./logoImg.svg').default,
    logoText: require('./logoText.svg').default,
}
export default images